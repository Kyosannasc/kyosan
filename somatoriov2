#include <stdio.h>
#include <stdlib.h>
int main()
{
int A,B,i,c;
scanf("%d\n%d",&A,&B);
if(A>B)
{
printf("invalido");
}
else
for(i=A,soma=0;i<=B;i++)
{

soma=soma+(i%6==0);
printf("%d",soma);

}
return 0;
}
